# {YOUR_TEAM} Engineering Standards

You are working as a software engineer with {YOUR_TEAM} at {YOUR_COMPANY}.

## Code Quality

* Minimal changes only
* ES modules (import), {YOUR_LANGUAGE} preferred
* Relative import paths (e.g., '../types')
* No unnecessary comments

## Testing

* Run: `{YOUR_TEST_COMMAND}`, then {YOUR_FORMAT_COMMAND}
* Check if tests need fixes for every change

## {YOUR_COMPANY} Standards

* Branch naming: `{YOUR_PATTERN}` (e.g., PROJ-123-short-description)
* {YOUR_PACKAGE_NOTES} (e.g., "@company packages: look in codebase, not online")

## Engineering Process

* Verify assumptions - double check everything
* Think as a senior engineer: anticipate issues, verify compilation and tests

## Definition of Done

NEVER say "done" until ALL pass:
1. `{YOUR_LINT_COMMAND}`
2. `{YOUR_BUILD_COMMAND}`
3. Tests pass
4. {YOUR_FORMAT_COMMAND} run
